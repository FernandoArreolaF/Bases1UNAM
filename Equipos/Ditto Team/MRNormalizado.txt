ARTICULO:{codBarras varchar(12) (PK), fotografía BYTEA, categoría varchar(15), nombre varchar(15), precioVenta float, precioCompra float, stock smallint}

PROVEEDOR:{rfcProv varchar(13) (PK), razSocialProv varchar(150), estado varchar(50), calle varchar(50), colonia varchar(50), número smallint, c.p. varchar(5), 
teléfonoProv varchar(10), ctaPago varchar(20)}

SURTIR:{[codBarras varchar(12) (FK), rfcProv varchar(13) (FK)] (PK)}

VENTA:{folio varchar(7) (PK), fecha timestamp, cantTotalArt smallint, montoTotal float (C), rfcCLiente varchar(13) (FK) NULL}

CLIENTE:{rfcCLiente varchar(13) (PK), nombres varchar(100), apPat varchar(50), apMat varchar(50) NULL, email varchar(100), razSocialCLiente varchar(150), 
teléfonoCliente varchar(10), estado varchar(50), calle varchar(50), colonia varchar(50), número smallint, c.p. varchar(5)}

TIENE:{[folio varchar(7) (FK), codBarras varchar(12) (FK)] (PK), cantXArt smallint, montoXArt float (C)}

EMPLEADO:{numEmp varchar(5) (PK), fechaIng date, tipoEmp varchar(50), rfcEmp varchar(13), curp varchar(18), email varchar(100), nombres varchar(100), 
apPat varchar(50), apMat varchar(50) NULL, estado varchar(50), calle varchar(50), colonia varchar(50), número smallint, c.p. varchar(5), numEmpSup varchar(5) (FK), 
teléfonoSuc varchar(10) (FK)}

TELÉFONO:{[numEmp varchar(5) (FK), teléfono varchar(10)] (PK)}

REALIZA:{[folio varchar(7) (FK), numEmp varchar(5) (FK)] (PK), numEmpCobrador varchar(5) (FK)}

SUCURSAL:{teléfonoSuc varchar(10) (PK), estado varchar(50), calle varchar(50), colonia varchar(50), número smallint, c.p. varchar(5), añoFund smallint}


NORMALIZACIÓN

Para ARTÍCULO:
Ya está en 1FN. También cumple 2FN. Para 3FN separamos "categoría" en otra tabla. Resultando en dos tablas.
ARTÍCULO:{codBarras varchar(12) (PK), fotografía BYTEA, idCat smallint (FK), nombre varchar(15), precioVenta float, precioCompra float, stock smallint}

CATEGORÍA:{idCat smallint (PK), categoría varchar(15)}

Para PROVEEDOR:
EStá en 2FN. Para 3FN separamos "estado" en otra tabla.
PROVEEDOR:{rfcProv varchar(13) (PK), razSocialProv varchar(150), idEdo smallint (FK), calle varchar(50), colonia varchar(50), número smallint, c.p. varchar(5), 
teléfono varchar(10), ctaPago varchar(20)}

ESTADO:{idEdo smallint (PK), estado varchar(50)}

Para SURTIR:
Ya está en 3FN.

Para VENTA:
Ya está en 3FN.

Para CLIENTE:
Ya está en 1FN, igual en 2FN. Para la 3FN podemos separar "estado" en otra tabla.
CLIENTE:{rfcCLiente varchar(13) (PK), nombres varchar(100), apPat varchar(50), apMat varchar(50) NULL, email varchar(100), razSocialCliente varchar(150), teléfonoCLiente varchar(10),
idEdo smallint (FK), calle varchar(50), colonia varchar(50), número smallint, c.p. varchar(5)}

Para TIENE:
Cumple 1FN. Para 2FN, aunque hay llave compuesta, no existen dependencias funcionales, por lo que cumple 2FN. Cumple 3FN.

Para EMPLEADO:
Cumple 1FN, también 2FN. Para 3FN separamos "estado" y "tipo" en otras tablas. Quedando:
EMPLEADO:{numEmp varchar(5) (PK), fechaIng date, idTipo smallint (FK), rfcEmp varchar(13), curp varchar(18), email varchar(100), nombres varchar(100), apPat varchar(50),
apMat varchar(50) NULL, idEdo smallint (FK), calle varchar(50), colonia varchar(50), número smallint, c.p. varchar(5), numEmpSup varchar(5) (FK), 
teléfonoSuc varchar(10) (FK)}

TIPO:{idTipo smallint (PK), tipo varchar(50)}

Para TELÉFONO:
Cumple 3FN.

Para REALIZA:
A pesar de ser llave compuesta cumple con 2FN. También con 3FN.

Para SUCURSAL:
Cumple 2FN. Para 3FN separamos "estado" en otra tabla.
SUCURSAL:{teléfonoSuc varchar(10) (PK), idEdo smallint (FK), calle varchar(50), colonia varchar(50), número smallint, c.p. varchar(5), añoFund smallint}
